<Window  x:Class="TPL_Store.views.OrderDetailsView"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         xmlns:local="clr-namespace:TPL_Store.views"
         mc:Ignorable="d"
         Title="OrderDetailsView" Height="450" Width="800">
    <Grid>
        <TextBlock Text="{Binding SelectedOrder.OrderID}" />
        <TextBlock Text="{Binding SelectedOrder.OrderedBy}" />
        <TextBlock Text="{Binding SelectedOrder.TotalCost}" />
    </Grid>
</Window>
